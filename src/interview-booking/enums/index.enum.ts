export enum InterviewBookingProcessType {
  PENDING = 'PENDING',
  MATCHED = 'MATCHED',
  CANCELLED = 'CANCELLED',
  FAILED = 'FAILED',
}

export enum InterviewType {
  PEERS = 'PEERS',
  FRIEND = 'FRIEND',
}

export enum SkillType {
  SOFT = 'SOFT',
  HARD = 'HARD',
}
